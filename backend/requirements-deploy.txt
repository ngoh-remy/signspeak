fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
python-multipart==0.0.6
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0

# NOTE: TensorFlow and MediaPipe are NOT included here because Render's free
# tier (512MB RAM) cannot load them at runtime. The backend will automatically
# start in Simulation Mode (see Ai_model/inference.py), which is designed for
# cloud demos. Real ML inference works locally with the full requirements.txt.
#
# Full requirements (for local dev): tensorflow==2.15.0, mediapipe==0.10.7,
# opencv-python-headless==4.8.1.78, numpy==1.24.3, scikit-learn==1.3.2
